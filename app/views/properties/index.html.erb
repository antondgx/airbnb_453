<%= link_to  "CreateProperty", new_property_path %>
<% @properties.each do |property| %>
   <%= property.title %>
   <%= property.description %>
   <%= property.price %>
   <%= property.address %>
   <%= property.capacity %>
   <%= link_to "show page", property_path(property) %>
<% end %>

<%= link_to "my bookings", bookings_path %>


